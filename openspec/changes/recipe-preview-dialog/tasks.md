## 1. Dialog Component

- [ ] 1.1 Create the `RecipePreviewDialog.vue` (or equivalent) component
- [ ] 1.2 Implement the read-only rendering of the recipe within the dialog using existing layout components
- [ ] 1.3 Add an "Edit Recipe" primary CTA button inside the dialog header or footer
- [ ] 1.4 Ensure the dialog has a close mechanism (e.g., close button, click outside to dismiss)

## 2. Integration and Routing

- [ ] 2.1 Add the new preview dialog component to the cookbook project view
- [ ] 2.2 Update the click handler on recipe cards in the cookbook view to open the preview dialog and pass the selected recipe, instead of triggering router navigation
- [ ] 2.3 Implement the navigation logic for the "Edit Recipe" button to close the dialog and transition to the full recipe editor route
