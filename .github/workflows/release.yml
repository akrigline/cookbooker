name: Test and Release

on:
  push:
    branches: [main]
    paths-ignore:
      - 'brainstorming/**'
      - 'openspec/**'
      - 'docs/**'
  workflow_dispatch:

permissions:
  contents: write
  actions: write

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run build

      # Kept in sync with the identical gate in ci.yml — see the comment there.
      - name: Assert ingredient locale survived tree-shaking
        run: |
          grep -rq 'gallon' dist/assets \
            || { echo "::error::Ingredient locale data missing from the bundle — it was tree-shaken out (see CLAUDE.md)"; exit 1; }

  release:
    needs: test
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GH_REPO: ${{ github.repository }}
      TZ: America/New_York
    steps:
      # Two Dependabot PRs merging close together each dispatch this workflow
      # at `--ref main`, so both runs resolve github.sha to the same tip commit
      # (the `release` concurrency group queues rather than cancels). Without
      # this guard the second run cuts a duplicate tag and redeploys the exact
      # same commit. `gh release create` makes lightweight tags, so a tag ref's
      # object.sha is the commit sha directly.
      - name: Skip if this commit is already released
        id: existing
        run: |
          # No `| head -n1` here: steps run with `-o pipefail`, and closing the
          # pipe early can hand `gh api --paginate` a SIGPIPE. Take the first
          # line in bash instead.
          MATCHES=$(gh api --paginate "repos/${GH_REPO}/git/matching-refs/tags/v" \
            --jq ".[] | select(.object.sha == \"${GITHUB_SHA}\") | .ref")
          EXISTING="${MATCHES%%$'\n'*}"

          if [[ -n "$EXISTING" ]]; then
            echo "::notice::${GITHUB_SHA} is already released as ${EXISTING#refs/tags/}; skipping."
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Compute next date-based tag
        id: tag
        if: steps.existing.outputs.skip == 'false'
        run: |
          TODAY=$(date +%Y.%m.%d)
          REFS_RAW=$(gh api "repos/${GH_REPO}/git/matching-refs/tags/v${TODAY}" --jq '.[].ref')

          REFS=()
          if [[ -n "$REFS_RAW" ]]; then
            mapfile -t REFS <<< "$REFS_RAW"
          fi

          MAX=0
          for REF in "${REFS[@]}"; do
            NAME="${REF#refs/tags/}"
            if [[ "$NAME" == "v${TODAY}" ]]; then
              N=1
            elif [[ "$NAME" =~ ^v${TODAY}-([0-9]+)$ ]]; then
              N="${BASH_REMATCH[1]}"
            else
              continue
            fi
            (( N > MAX )) && MAX=$N
          done

          if (( MAX == 0 )); then
            TAG="v${TODAY}"
          else
            TAG="v${TODAY}-$((MAX + 1))"
          fi

          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        if: steps.existing.outputs.skip == 'false'
        run: |
          gh release create "${{ steps.tag.outputs.tag }}" \
            --target "${{ github.sha }}" \
            --title "${{ steps.tag.outputs.tag }}" \
            --generate-notes

      - name: Trigger Pages deploy
        if: steps.existing.outputs.skip == 'false'
        run: gh workflow run pages.yml --ref main -f tag="${{ steps.tag.outputs.tag }}"
